# CLAUDE.md — 設計・コード担当への指示

> このファイルは **Claude が自動で読む**指示書です。
> グラフィック担当（ChatGPT / Codex）側は [`AGENTS.md`](AGENTS.md) を読みます。

## プロジェクト

『**昆虫戦争**』— iPhone の Safari で遊ぶ、昆虫モチーフのターン制ストラテジー（PWA）。
子供が遊びながら昆虫と自然を学べることを目指す。全8世界 × 10ステージ。

- 企画の全体像: [`PLAN.md`](PLAN.md)
- ゲーム本体の解説: [`game/README.md`](game/README.md)
- **著作権は野村 邦弘に帰属**（[`LICENSE`](LICENSE)）。オープンソースではない

## 役割分担

| | 担当 | 触ってよいもの |
|---|---|---|
| **Claude（あなた）** | 設計・ゲームのコード・仕様書 | `game/js/**`, `game/css/**`, `game/index.html`, `game/test/**`, `PLAN.md`, `game/ART_SPEC.md`, `game/art/**`, `scripts/**`, `.github/**` |
| **ChatGPT / Codex** | グラフィックの生成と納品 | `game/assets/**` |

**`game/assets/` 配下は原則さわらない。** 絵はグラフィック担当の領分。
仕様を変えたいときは、`game/assets/` の中身ではなく **`game/art/orders.json` と
`game/ART_SPEC.md` を更新**して、発注内容として伝える。

## 設計の原則（崩さない）

1. **ルールと見た目を分ける**
   - `game/js/engine.js` … ゲームのルール。描画も入力も知らない純ロジック
   - `game/js/render.js` … 盤面の描画。ルールを知らない
   - `game/js/battle.js` … 戦闘演出。**勝敗を計算しない**。engine の結果を再生するだけ
   - `game/js/main.js` … 画面遷移と入力のつなぎ役
2. **エンジンとデータを分ける**
   - 80面のマップ追加は `game/js/data/maps.js` に足すだけ
   - バランス調整は `game/js/data/units.js` の数値だけで完結させる
   - **1面ごとの手調整はしない**（80面あるので共通の数値で成立させる）
3. **強さの数値には必ず生態上の根拠をつける**
   - 例: カマキリが空の虫と戦えるのは、実際に飛翔中の虫を鎌で捕らえるから
   - `bio.why` にその理由を書き、図鑑にそのまま出す

## 変更したら必ずテストを通す

```sh
# ルールの単体テスト（ブラウザ不要・マップデータの検証も兼ねる）
node game/test/engine.test.mjs

# 画面のながれ（iPhone サイズの実ブラウザ）
npx http-server game -p 8123 -c-1 &
GAME_URL=http://localhost:8123 node game/test/ui.test.mjs
```

- Playwright は `node_modules` をグローバルにシンボリックリンクして使っている（`.gitignore` 済み）
- UI テストはリポジトリ直下から実行すること（ESM が `playwright` を解決できないため）
- 新しいルールを足したら、**不変条件のテストも一緒に足す**
  （例: 寄生が無限に続かない、季節と寄生の補正を重ねても攻撃力が負にならない）

## グラフィックの受け取り方

- スプライトは `game/assets/units/<ユニットID>.png`。**なければ絵文字にフォールバック**する
- `manifest.json` は `scripts/build-manifest.mjs` が自動生成する（手で書かない）
- 受け入れ検査は `scripts/check-assets.mjs`（透過・サイズ・占有率・余白）
- 絵が届いたら、**実際のゲーム画面に並べて 48px での判別性を確認**する

## 書き方

- コード内のコメント・UI の文言・図鑑の文章は**すべて日本語**。対象は小学校低〜中学年
- UI はひらがな主体（「せんりょう」「たたかい方」）。漢字にはふりがなを想定
- 全ソースファイルの先頭に著作権表示を入れる

## やらないこと

- 任天堂の権利物（「ファミコンウォーズ」の名称・ロゴ・グラフィック・音楽）は使わない
- 外部通信・広告・トラッキングは入れない（子供向けのため）
- 流血や死の描写はしない。倒れた虫は「にげていく」
  - ただし**自然界の厳しさはごまかさない**（ハリガネムシに操られた虫は水に沈む）
