# 作業のしかた — Claude と ChatGPT の連携

**設計とコードは Claude、グラフィックは ChatGPT。**
その2つを、**あなたが毎回コピペしなくても回るように**組んだ仕組みの説明です。

---

## 1. 考え方: リポジトリを唯一の作業場にする

Claude と ChatGPT は別の会社の別の製品なので、**両者を直接つなぐAPIはありません**。
そこで、**GitHub リポジトリを「共有の机」にして、そこ越しに受け渡し**します。

```
                    ┌─────────────────────────────┐
                    │   GitHub リポジトリ          │
                    │   （唯一の共有の机）          │
                    └─────────────────────────────┘
                        ▲                    ▲
       設計・コード       │                    │      グラフィック
                        │                    │
   ┌────────────────────┴───┐      ┌────────┴────────────────────┐
   │  Claude                │      │  ChatGPT / Codex            │
   │  CLAUDE.md を読む       │      │  AGENTS.md を読む            │
   │                        │      │                             │
   │  発注書を書く           │─────▶│  発注書を読んで絵を作る       │
   │  game/art/orders.json  │      │  game/assets/units/*.png    │
   │                        │◀─────│  として納品                  │
   │  絵を受け取って組み込む  │      │                             │
   └────────────────────────┘      └─────────────────────────────┘
                        │                    │
                        └────────┬───────────┘
                                 ▼
                    ┌─────────────────────────────┐
                    │  GitHub Actions（自動）      │
                    │  ・受け入れ検査              │
                    │  ・manifest.json の更新      │
                    └─────────────────────────────┘
```

**ポイントは、両方のAIが「自分の指示書」を自動で読むこと**です。
あなたが毎回「こういうルールで作って」と説明する必要はありません。

| ファイル | 誰が自動で読むか | 中身 |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | **Claude** | 設計の原則、触ってよい範囲、テストの回し方 |
| [`AGENTS.md`](AGENTS.md) | **ChatGPT / Codex** | 絵の共通仕様、納品の形式、PRの出し方 |
| [`game/art/orders.json`](game/art/orders.json) | **両方** | 発注書（機械可読）。ID・学名・占有率・背景色・プロンプト |

`AGENTS.md` は OpenAI Codex がリポジトリ直下から自動で読み込む慣習のファイル名、
`CLAUDE.md` は Claude が自動で読むファイルです。**同じ机の上に、両者の作業指示書が置いてある**状態にしてあります。

---

## 2. ChatGPT に渡すもの（会話のコピペは不要です）

**この会話ややりとりの履歴を ChatGPT に貼り付ける必要はありません。**
むしろ貼らないほうがうまくいきます。理由は3つ:

- 長すぎて、途中の検討過程や却下した案まで混ざる（誤解のもとになる）
- **すぐ古くなる**。仕様を変えるたびに貼り直すことになり、コピペを無くす目的に反する
- 必要な情報は**すでにリポジトリの中にある**（`AGENTS.md` と `orders.json`）

渡すのは **リポジトリのURLと、下の短い文だけ**です。

### 先に必要な準備

**ありません。** `AGENTS.md`・`game/art/orders.json`・発注書（`game/art/briefs/`）は
**すべて `main` にあります**（寄生4体を含む全12体ぶん）。
ChatGPT にリポジトリを見せれば、そのまま読めます。

### ChatGPT に貼る文（これだけ）

はじめて渡すとき:

```
このリポジトリ https://github.com/Kunihiro-Nomura/monday-kun の
グラフィック担当をお願いします。

まず AGENTS.md を読んでください。
あなたの役割・守るべき絵の仕様・納品の形式がすべて書いてあります。
発注書は game/art/orders.json です（機械可読）。

game/assets/units/kabuto.png が承認ずみの基準絵です。
線の太さ・彩度・光の向きを、これに揃えてください。

最初の仕事:
orders.json の inGame が true で status が todo のものを、
priority 順に1体ずつ作ってください。1体できるたびに見せてください。

納品先: game/assets/units/<id>.png（96×96・透過PNG）
game/assets/ 以外のファイルは変更しないでください。
```

2回目以降は、これだけで通じます:

```
orders.json の続きを priority 順にお願いします。
kabuto.png をスタイル参照にしてください。
```

### GitHub につながらないと言われたら

`game/art/briefs/` に、**リポジトリを読まなくても仕様が全部伝わる発注書**を
置いてあります。中身をそのままチャットに貼れば作業できます。

| ファイル | 中身 |
|---|---|
| `A-世界1のこり7体.md` | アリ・カマキリ・テントウムシ・クワガタ・ミイデラゴミムシ・スズメバチ・オニヤンマ |
| `B-寄生4体.md` | コマユバチ・ヤドリバエ・ハリガネムシ・タイワンアリタケ |

英語プロンプトと日本語版が虫ごとに入っています。**A から先に**渡してください
（世界1が先に遊ばれるので、そこに出る虫のほうが効きます）。

自分で作り直すこともできます:

```sh
node scripts/make-brief.mjs ant mantis      # 指定した虫だけ
node scripts/make-brief.mjs --todo          # まだ届いていないぶん全部
```

### うまくいかないときの確認

| 症状 | 原因 |
|---|---|
| 「AGENTS.md が見つからない」と言われる | ブランチが `main` になっていない（上の準備） |
| 絵の仕様を無視してくる | `AGENTS.md` を読んでいない。「AGENTS.md を読んで」と明示する |
| チーム色（青／赤）で塗ってくる | よくある誤解。「虫は実物の色のまま。青赤はゲーム側でつける」と伝える |
| 検査が落ちる | PR のログに日本語で理由が出るので、それをそのまま ChatGPT に伝える |

---

## 3. ChatGPT 側のつなぎ方（最初に1回だけ）

やり方は2つあります。**自動化の度合いが高いのは A** です。

### A. ChatGPT の Codex にリポジトリを接続する（おすすめ）

1. ChatGPT の Codex で、この GitHub リポジトリを接続する
2. 「`AGENTS.md` を読んで、`game/art/orders.json` の発注をこなして」と一度伝える
3. 以後、Codex がリポジトリを読んで作業し、**PR を出してくる**

この形なら、あなたの操作は**PR を承認するだけ**です。

> **正直な制約**: Codex はコードを書く環境なので、**画像生成そのものはできません**。
> 絵は ChatGPT の画像生成側で作り、Codex（または後述の手動アップロード）でリポジトリに入れる、
> という2段構えになります。ここだけは完全自動になりません。

### B. 「貼るだけ発注書」を使う（GitHub につながらない場合）

**ChatGPT の実行環境から GitHub に接続できないことがあります。**
その場合は、リポジトリを読ませずに済む**自己完結した発注書**を生成して貼り付けます。

```sh
node scripts/make-brief.mjs kabuto        # 1体だけ
node scripts/make-brief.mjs kabuto ant    # 複数まとめて
node scripts/make-brief.mjs --todo        # まだ届いていないぶん全部
node scripts/make-brief.mjs               # 最優先の1体だけ
```

出力には**共通仕様・学名・分類の注意・占有率・背景色・英日のプロンプト・受け入れ基準**が
すべて入っているので、**そのまま貼るだけ**で伝わります。
`orders.json` から生成しているので、仕様を変えたら生成し直すだけで最新になります
（手で書き写す必要はありません）。

昆虫でないもの（ハリガネムシ＝類線形動物、タイワンアリタケ＝菌類、食虫植物＝植物）には
**警告が自動で入ります**。学習教材なので、ここを取り違えられるのがいちばん困るためです。

### C. ChatGPT の画像生成で作って、GitHub にアップロードする

1. ChatGPT に「このリポジトリの `game/art/orders.json` を見て」と伝える
   （ChatGPT の GitHub コネクタでリポジトリを読めます）
2. 絵を生成させる
3. できた PNG を GitHub の Web 画面から **`game/assets/units/` にドラッグ&ドロップ**

**ファイル名さえ合っていれば、あとは全部自動**です（次章）。

---

## 4. 絵が届いたあと、自動で起きること

`game/assets/units/` に PNG が push されると、GitHub Actions
（[`.github/workflows/art.yml`](.github/workflows/art.yml)）が自動で走ります。

### 自動その1: `manifest.json` の更新

ゲームは `manifest.json` に載っているスプライトだけを読み込みます。
このファイルは **`scripts/build-manifest.mjs` が自動で作り直してコミット**します。

→ **あなたも ChatGPT も、manifest を手で書く必要はありません。**

### 自動その2: 受け入れ検査

`scripts/check-assets.mjs` が、目視でなく**機械で**仕様どおりかを判定します。

| 検査 | 落ちる例 |
|---|---|
| PNG として読めるか | 壊れたファイル |
| **透過されているか** | 背景が不透明 → チーム色の台座が隠れてしまう |
| サイズが 96×96 か | 1024×1024 のまま納品された |
| **占有率が指定どおりか** | アリが指定35%なのに85%で描かれた（カブトムシと同じ大きさになってしまう） |
| 上下左右に5%以上の余白があるか | 端で切れている |
| **背景色が絵に残っていないか** | 緑背景で切り抜いて、上翅がオリーブ色に濁った（カブトムシの初回納品） |

落ちると PR が赤くなり、**何がどう違うか日本語で表示**されます。

```
  NG  ant: 占有率が 85%（指定は 35%±5）。大きすぎます
  NG  mantis: 四隅のうち 4か所が 透明では ありません。背景が 残っています
  ok  kabuto: 96×96 / 占有率 85% / 余白 7.3%
```

### 自動その3: ゲームが壊れていないか

GitHub Actions が2つ動いています。

| ファイル | いつ動く | 何をする |
|---|---|---|
| `.github/workflows/art.yml` | `game/assets/` か `orders.json` を変えたとき | manifest 更新／絵の受け入れ検査 |
| `.github/workflows/test.yml` | `game/` か `scripts/` を変えたとき | ルール64件／画面47件／むずかしさの計測 |

画面のテストは実際に Chromium を iPhone サイズで動かします。
**落ちたときはスクリーンショットが Actions の成果物として残る**ので、
何が映っていたかを見てから直せます。

落ちた理由は日本語で出るので、そのログをそのまま Claude や ChatGPT に渡せます。

---

## 5. あなたがやること

| 場面 | やること |
|---|---|
| 最初に1回 | ChatGPT に「`AGENTS.md` を読んで」とリポジトリを渡す |
| 絵ができたら | PNG を `game/assets/units/` に置く（またはPRを承認する） |
| PR が赤くなったら | ChatGPT に「検査が落ちたので直して」と伝える（ログはPRに出ています） |

**手で書く必要がなくなったもの**: manifest.json、プロンプト、受け入れチェックリスト、
どの絵がまだ届いていないかの管理。

---

## 6. 手元で確認するコマンド

```sh
# 絵が仕様どおりか
node scripts/check-assets.mjs

# manifest を作り直す
node scripts/build-manifest.mjs

# ゲームのテスト
node game/test/engine.test.mjs
npx http-server game -p 8123 -c-1 &
GAME_URL=http://localhost:8123 node game/test/ui.test.mjs
```

---

## 7. 衝突しないための決めごと

### 同じ絵を2つの経路で発注しない

A（コネクタ経由）と B（貼るだけ発注書）は**併用できます**が、
**同じ ID を両方から同時に頼まないでください**。
別々の絵が2枚返ってきて、どちらをスタイル基準にするか決められなくなります。

とくに最初の `kabuto` は**以降すべての絵の基準**になるので、必ず1枚に絞ります。
どちらの経路で頼んだかは `orders.json` の `status` で管理します
（発注したら `ordered`、届いたら `done`）。

### 担当範囲

同じリポジトリを2つのAIが触るので、**担当範囲を分けて**あります。

| | 触ってよい | 触らない |
|---|---|---|
| **Claude** | `game/js/`, `game/css/`, `game/test/`, `PLAN.md`, `game/art/`, `scripts/`, `.github/` | `game/assets/` |
| **ChatGPT** | `game/assets/` | それ以外すべて |

相手の領分に直したい点があれば、**直接書き換えずに PR のコメントか Issue で指摘**します。
これで、お互いの変更が上書きし合う事故を防げます。

---

## 8. まだ完全自動にならない部分（正直な話）

- **画像生成そのもの**は、ChatGPT 側の操作が要ります（Codex の環境では画像を生成できないため）
- **スタイルの承認**は人が見て決める必要があります
  （まず `kabuto` を1体だけ作り、それを承認してからスタイル参照として残りを作る流れにしてあります）

逆に言うと、**それ以外の受け渡し・検査・組み込みは自動化済み**です。
