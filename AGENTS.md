# AGENTS.md — グラフィック担当エージェントへの指示

> このファイルは **OpenAI Codex / ChatGPT 側のエージェントが自動で読む**ための指示書です
> （`AGENTS.md` は Codex がリポジトリ直下から自動的に読み込む慣習のファイル名）。
> 人間がコピペしなくても、リポジトリを開いた時点でこの内容が伝わることを狙っています。
>
> Claude 側の担当範囲は [`CLAUDE.md`](CLAUDE.md) にあります。

## このプロジェクトについて

『**昆虫戦争**』— iPhone の Safari で遊ぶ、昆虫をモチーフにしたターン制ストラテジー。
子供が遊びながら昆虫と自然を学べることを目指しています。

- 企画の全体像: [`PLAN.md`](PLAN.md)
- **著作権は野村 邦弘に帰属します**（[`LICENSE`](LICENSE)）。オープンソースではありません

## 役割分担

| | 担当 | 触るもの |
|---|---|---|
| **Claude** | 設計・ゲームのコード・仕様書の作成 | `game/js/**`, `game/css/**`, `game/index.html`, `PLAN.md`, `game/ART_SPEC.md`, `game/art/orders.json`, `scripts/**` |
| **あなた（ChatGPT / Codex）** | **グラフィックの生成と納品** | `game/assets/**` のみ |

**重要**: あなたは `game/assets/` 以外のファイルを変更しないでください。
ゲームのコードや仕様書に直したい点があれば、**変更せずに Issue か PR のコメントで指摘**してください。
Claude 側が受け取って直します。役割を分けることで、お互いの作業が衝突しません。

---

## あなたの仕事の進め方

### 1. 発注を読む

**[`game/art/orders.json`](game/art/orders.json) が唯一の発注書**です（機械可読）。
人間向けの解説は [`game/ART_SPEC.md`](game/ART_SPEC.md) にありますが、
**数値や文言が食い違う場合は `orders.json` を正**としてください。

`orders.json` の各項目:

```jsonc
{
  "id": "kabuto",              // 納品するファイル名（kabuto.png）
  "name": "カブトムシ",
  "scientificName": "Trypoxylus dichotomus",
  "classification": "insect",  // insect / plant / fungus / nematomorph など
  "sizeClass": "large",
  "occupancy": 0.85,           // キャンバスに占める割合（±0.05 まで可）
  "background": "green",       // 生成時の背景色（green / magenta）
  "featureJa": "...",          // プロンプトに差し込む特徴（日本語）
  "featureEn": "...",          // 同（英語・こちらのほうが安定します）
  "status": "todo"             // todo / done — 納品したら done にしてください
}
```

### 2. 絵を作る

プロンプトのひな形は [`game/ART_SPEC.md`](game/ART_SPEC.md) §2 にあります。
**必ず守ってほしい共通仕様**（詳細は同 §1）:

- **真上からの俯瞰**（標本写真と同じ見え方）。頭が**画面の上**
- 左右の重心を揃えた自然な姿勢（機械的な完全対称にはしない）
- **擬人化しない**（顔・表情・人間の手足をつけない）
- 濃い輪郭線をはっきり。光は左上から。**影は描かない**（影はゲーム側でコードが付けます）
- 実物に忠実な自然色。**チーム色（青／赤）に塗り替えない**
  （青軍・赤軍はゲーム側が台座で表現します。虫は実物の色のまま1体だけ作ってください）
- **背景は `background` の指定色**。緑の被写体を緑背景にすると抜けなくなるので必ず従ってください

**背景と透過について（実際の納品で分かったこと）**:

- **可能なら生成時点で透過PNGを直接出力してください。** そのほうが確実です
- 単色背景から切り抜く場合、**つやのある暗い色の被写体（カブトムシなど）は、
  光沢面に背景の緑が映りこんで上翅がオリーブ色に濁ります**（色かぶり／green spill）。
  切り抜くときは必ず**色かぶりの除去**を行ってください
- 実物の色から外れると学習教材として困るので、切り抜き後に色を確認してください

**分類の注意**: `classification` が `insect` 以外のものは昆虫ではありません。
学習教材なので、ここを間違えると致命的です。

- `nematomorph`（ハリガネムシ）— 脚も翅も触角もない、細長い線形動物として描く
- `fungus`（タイワンアリタケ）— アリに寄生した**菌類の子実体**。虫として描かない
- `plant`（ハエトリグサ・モウセンゴケ・ウツボカズラ）— 植物として描く

**スタイルの統一が最重要です。** `game/assets/reference/` に承認済みの基準画像があれば、
必ずそれをスタイル参照として使い、線の太さ・彩度・光の向きを揃えてください。

### 3. 納品する

1. 背景を抜いて**透過 PNG** にする
2. **96×96** にリサイズする（被写体を中央に、`occupancy` の割合で配置）
3. `game/assets/units/<id>.png` として置く
4. `orders.json` の該当項目の `status` を `"done"` にする
5. 高解像度の元画像を残す場合は `game/assets/source/<id>.png`（任意・ゲームには使われません）
6. **PR を作る**（`main` に直接 push しない）

`manifest.json` は**手で編集しないでください**。push すると GitHub Actions が自動で作り直します。

### 4. 自動チェックが走ります

PR を作ると `.github/workflows/art.yml` が動き、次を自動で検査します。

- PNG として読めるか
- **透過されているか**（不透明だとチーム色の台座を隠してしまうため必須）
- サイズが 96×96 か
- **占有率**が `orders.json` の指定と合っているか（±0.05）
- 上下左右に**最低5%の余白**があるか

落ちた項目は PR のログに出ます。直して push し直してください。
手元で確認したいときは:

```sh
node scripts/check-assets.mjs
```

---

## 納品してほしい順番

1. **`kabuto`（カブトムシ）** — これがスタイル基準になります。まずこれ1体だけ作って承認を得てください
2. 承認後、盤面ユニットの残り（`orders.json` の `status: "todo"` を上から）
3. 戦闘シーン用の横向きスプライト（[`game/ART_SPEC.md`](game/ART_SPEC.md) §3.2）
4. 図鑑用イラスト（同 §3.3）

---

## 権利について

- 生成に使ったモデル名・生成日・プロンプトを **`game/CREDITS.md` に必ず記録**してください
- 使用する画像生成AIの**利用規約で商用利用が許されているか**を確認してください
- 生成物の**著作権は野村 邦弘に帰属**させます
